var numbers;
var playerAnswer;
var add;

prompt ("Solve 10 + 2");

function add (num1, num2) {
    return num1 + num2;
}
 if (playerAnswer == add){
     document.write ("You're Correct");
 }else {
     document.write("Sorry! Maybe next time!");
 }
